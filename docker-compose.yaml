version: "3.9"

services:
  monitor-api:
    build: .
    container_name: api-health-monitor
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DB_PATH: "/data/monitor.db"
    volumes:
      - ./data:/data