services:
  monitor-api:
    build: .
    container_name: api-health-monitor
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DB_PATH: "/data/monitor.db"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/data

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./deployments/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    depends_on:
      - monitor-api

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./deployments/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./deployments/monitoring/grafana-dashboard.json:/var/lib/grafana/dashboards/api-health-monitor.json:ro
    depends_on:
      - prometheus

volumes:
  grafana_data: